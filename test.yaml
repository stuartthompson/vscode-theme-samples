key: {{environment.name}}

trunk:
  branch:
    leaf: [ one, of, many ]
  second-branch:
    another-leaf: simple

some:
  boolean: true

deeper:
  keys:
    name: Value
    description: has words
    lists:
      - uses: actions/checkout@v2
      - foo: bar
        with: data
        and:
            fizz: buzz
            test: lists, values, responses